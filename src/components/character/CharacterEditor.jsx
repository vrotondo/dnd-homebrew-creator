// Check src/components/character/CharacterEditor.jsx

// If the file exists, make sure it has a default export:
// At the end of the file, ensure you have:
export default CharacterEditor;

import React from 'react';

const CharacterEditor = () => {
    return (
        <div className="container mx-auto p-4">
            <h1 className="text-2xl font-bold mb-4">Character Editor</h1>
            <p>Character editor component (under construction)</p>
        </div>
    );
};

